import { translations } from '~/locales/translation'
import { _t } from '~/utils/lang'

export const lang = {
  heading: () => _t(translations.notfound.heading),
  subheading: () => _t(translations.notfound.subheading),
  back: () => _t(translations.notfound.back)
}
